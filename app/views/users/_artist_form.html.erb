<%= form_for(@user, :html => { :class => "artistForm"}) do |f| %>
	<h2>Developer?</h2>
	<p>Are you a developer? Get updates on our project and regular donation reports made to your email address below!</p>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.text_field :email, :placeholder => "Enter your email" %>
	<%= f.hidden_field :usertype, :value => "2" %>
	<%= f.submit "Submit", class: "btn" %>
	<p>These reports will include validation of your published donation address(s) and the relative perfomance of donations by project.</p>
<% end %>